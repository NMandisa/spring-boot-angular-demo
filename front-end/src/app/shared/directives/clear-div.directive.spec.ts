import { ClearDivDirective } from './clear-div.directive';

describe('ClearDivDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearDivDirective();
    expect(directive).toBeTruthy();
  });
});
